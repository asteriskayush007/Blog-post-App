<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Data-Association</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-900 w-full h-screen">
    <div class="w-full h-screen bg-zinc-900 text-white mt-10 p-3 align-center justify-center">
        <center><h1 class="text-5xl underline text-blue-500 tracking-tighter">Profile</h1></center>
        <span class="bg-zinc-700 float-right p-3 rounded-xl border-none"><a href="/logout" class="style-none text-red-500">Logout</a></span>

        <h1 class="text-3xl"><span class="text-green-500 bg-zinc-500 rounded-md p-1">Hello</span> , <%= user.name %> 🙋‍♂️</h1>
        <h5 class="mt-5">Now, You can Create a new Post.</h5>
        <form action="/post" method="post">
            <textarea name="content" placeholder="What's in your mind??" class="resize-none mt-10 p-3 text-2xl block h-[30vh] w-1/3 outline-none bg-transparent border-2 border-zinc-800 rounded-md"></textarea>
            <input type="submit" name="create" id="" value="Create Post" class="bg-blue-500 p-3 block rounded-xl border-none mt-5">
        </form>

        <div>
            <h3 class="text-2xl text-zinc-500 mt-10">Your Recents Post</h3>
            <div class="post-container mt-5">
                <% if(user.posts.length > 0){ %>
                    <% user.posts.reverse().forEach(function(post){ %>
                        <div class="post w-1/3 p-4 rounded-md border-[1px] mb-4 border-blue-800 bg-zinc-800">
                            <h4 class="text-blue-500 mb-3">@<%= user.username %></h4>
                            <p class="text-sm tracking-tight"><%= post.content %></p>
                            <small class="mt-2 inline-block"><%= post.likes.length %>❤️</small>
                            <div class="btn flex mt-1 gap-5">
                                <a href="/like/<%= post._id %>" class="text-blue-500">
                                    <%= post.likes.indexOf(user._id) === -1 ? "Like" : "Unlike" %>
                                </a>
                                <a href="/edit/<%= post._id %>" class="text-zinc-500">Edit</a>
                                
                            </div>
                        </div>
                    <% }) %>
                <% } else{%> 
                    <h1 class="text-3xl text-red-500">NO POSTS ARE PUBLISHED!!</h1>
                <% }%>

                <!-- <% user.posts.reverse().forEach(function(post){ %>
                    <div class="post w-1/3 p-4 rounded-md border-[1px] mb-4 border-blue-800 bg-zinc-800">
                        <h4 class="text-blue-500 mb-3">@<%= user.username %></h4>
                        <p class="text-sm tracking-tight"><%= post.content %></p>
                        <small class="mt-2 inline-block"><%= post.likes.length %>❤️</small>
                        <div class="btn flex mt-1 gap-5">
                            <a href="/like/<%= post._id %>" class="text-blue-500">
                                <%= post.likes.indexOf(user._id) === -1 ? "Like" : "Unlike" %>
                            </a>
                            <a href="/edit/<%= post._id %>" class="text-zinc-500">Edit</a>
                        </div>
                    </div>
                <% }) %> -->
            </div>
        </div>
    </div>
</body>
</html>