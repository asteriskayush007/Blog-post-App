<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Data-Association</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-900 w-full h-screen">
    <div class="w-full h-screen bg-zinc-900 text-white mt-10 p-3 align-center justify-center">
        <center><h1 class="text-5xl underline text-blue-500 tracking-tighter">Posts</h1></center>
        <span class="bg-zinc-700 float-right p-3 rounded-xl border-none"><a href="/logout" class="style-none text-red-500">Logout</a></span>
        <h5 class="mt-5">Now,You can Edit Your Post.</h5>
        <form action="/update/<%= post._id %>" method="post">
            <textarea name="content" placeholder="Edit Your Post" class="resize-none mt-10 p-3 text-2xl block h-[30vh] w-1/3 outline-none bg-transparent border-2 border-zinc-800 rounded-md"> <%= post.content %></textarea>
            <input type="submit" name="create" id="" value="Update Post" class="bg-yellow-500 text-black p-3 block rounded-xl border-none mt-5">
        </form>
    </div>
</body>
</html>